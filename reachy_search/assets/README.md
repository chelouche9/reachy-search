Drop `demo.mp4` here — a 15–30s clip of a question and answer, showing one of
the thinking moves. The Space landing page (`../../index.html`) autoplays it as
the hero, and falls back to a static block if it's missing.

Keep it muted-friendly: the antenna spin should read without sound.
